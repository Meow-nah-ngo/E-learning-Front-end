import Badge from "./Badge";

export default function SubjectFilter() {
    const subjects = [
        "Math", "Science", "History", "Art", "Music", "Physical Education", "Computer", "Economics", "Geography", "Philosophy", ""
    ]
}